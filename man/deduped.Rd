% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deduped.R
\name{deduped}
\alias{deduped}
\title{Deduplicate a function on its first argument}
\usage{
deduped(f)
}
\arguments{
\item{f}{Function to deduplicate.}
}
\value{
Deduplicated function
}
\description{
Deduplicate a function on its first argument
}
\examples{

x <- sample(LETTERS, 10)
x

large_x <- sample(rep(x, 100))
length(large_x)

long_func <- function(x) for(i in x) {Sys.sleep(0.001)}

system.time({y <- long_func(large_x)})
system.time({y2 <- deduped(long_func)(large_x)})
all(y == y2)
}
